/**
 * middleware.ts
 * authentication and authorization middlewares
 */

type SomeFunction = () => void

const IsAuthenticated = (r: Request, next: SomeFunction) => {
    // TODO implement IsAuthenticated middleware
}