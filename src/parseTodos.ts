import File from "./File";
import Todo from "./Todo";

export const parseTodos = (path: string) => {
  // regex here
  // parse // TODO ...
  // make a new Todo instance everytime
  // make a new File instance with its todos
  /**
   * return format:
   * [
   *   {
   *    fileName: ...,
   *    todos: [
   *      {
   *        todoText: ...
   *      }
   *    ]
   *   }
   * ]
   */
};
